server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    mail:
      enabled: false

spring:
  application:
    name: soporte-portales-salud

  datasource:
    url: jdbc:mysql://localhost:3306/soporte_portales_salud?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Santiago
    username: soporte_salud
    password: "SoportePortalesSalud.2026!"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 25MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: portalesssm.notifica@gmail.com
    password: "CLAVE_APP_GMAIL_SIN_ESPACIOS"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

app:
  upload:
    dir: /var/lib/soporte-portales-salud/uploads
    max-files: 5
    max-file-size-bytes: 5242880

  portal:
    public-base-url: http://localhost:4200
    backoffice-base-url: http://localhost:4200

  mail:
    enabled: true
    from: portalesssm.notifica@gmail.com
    from-name: Soporte Portales Salud DSSM
    reply-to: portalesssm.notifica@gmail.com

  security:
    jwt-secret: "CAMBIAR-JWT-SECRET-LARGO-PRUEBAS-PORTALES-SALUD-DSSM-2026"
    jwt-expiration-minutes: 480

  cors:
    allowed-origins: http://localhost:4200,http://localhost:8888

  bootstrap-admin:
    enabled: false
    full-name: Administrador Portales Salud
    email: admin@dssm.cl
    password: "Cambiar.2026!"
