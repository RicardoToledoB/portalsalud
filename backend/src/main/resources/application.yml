server:
  port: 8080

spring:
  application:
    name: soporte-portales-salud
  datasource:
    url: jdbc:mysql://localhost:3306/soporte_portales_salud?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Santiago
    username: soporte_salud
    password: SoportePortalesSalud.2026!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: America/Santiago
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 25MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: portalesssm.notifica@gmail.com
    # Para pruebas locales, pegue aquí la contraseña de aplicación de Gmail SIN espacios.
    # No use la contraseña normal de Gmail. No suba este archivo a Git con claves reales.
    password: qgqpguqtapnpwjge
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

app:
  upload:
    dir: uploads/solicitudes
    max-files: 5
    max-file-size-bytes: 5242880
  portal:
    public-base-url: http://localhost:4200
    backoffice-base-url: http://localhost:4200
  mail:
    enabled: true
    from: portalesssm.notifica@gmail.com
    from-name: Soporte Portales Salud DSSM
    reply-to: portalesssm.notifica@gmail.com
  security:
    # Cambiar en producción por una clave institucional larga. Mínimo recomendado: 32 caracteres.
    jwt-secret: CAMBIAR-ESTA-CLAVE-EN-PRODUCCION-DSSM-PORTALES-SALUD-2026
    jwt-expiration-minutes: 480
  cors:
    allowed-origins: http://localhost:4200,https://soporteportales.dssm.cl
  bootstrap-admin:
    enabled: true
    full-name: Administrador Portales Salud
    email: admin@dssm.cl
    password: Cambiar.2026!

management:
  endpoints:
    web:
      exposure:
        include: health,info
